import { VideoCommentsList } from './video-comments-list';

describe('VideoCommentsList', () => {
  it('should create an instance', () => {
    expect(new VideoCommentsList()).toBeTruthy();
  });
});
